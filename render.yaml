services:
  - type: web
    name: edusphere-backend-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5001
      - key: NODE_VERSION
        value: 22.14.0
